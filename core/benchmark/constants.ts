/** Default benchmark configuration values */
export const DEFAULT_ITERATIONS = 100
export const DEFAULT_TIMEOUT_MS = 5000
export const DEFAULT_WARMUP_ITERATIONS = 5
export const DEFAULT_WARMUP_ENABLED = true
export const DEFAULT_WARMUP_TIME_MS = 1000

/** Default reporter configuration values */
export const DEFAULT_REPORTER_FORMAT = 'console'
